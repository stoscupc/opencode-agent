Run as a PR merge session: verify PRs in isolated worktrees through a gated workflow — build, test, lint, code review, description quality — before approving for merge. **Workstream:** `dev-pr-merge`
